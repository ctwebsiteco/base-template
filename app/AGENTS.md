# App Router

See [agents/agents.md](agents/agents.md) for task-specific guides.

Quick reference:

| Question | Answer |
|----------|--------|
| Fetch data in RSC or useEffect? | RSC only |
| Metadata in layout or per-page? | Per-page `export const metadata` |
| Server Actions location? | `app/actions/` |
| Draft mode token source? | Env var `DRAFT_MODE_SECRET` |